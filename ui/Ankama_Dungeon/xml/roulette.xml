<!-- start of top nav -->
<div>
    <div class="background-primary bg-opacity-60 ">
        <ul class="flex mx-auto px-2 lg:px-0 justify-between font-semibold lg:w-4/5 text-xl text-primary py-1 flex-row h-24 lg:h-12">
            <li class="flex flex-row w-full justify-between">
                <!-- content rendered from socails from the portal 'socials' -->
                {%if socials.discord|length%}
                <div class="flex mr-auto">
                    <img class="w-8" src="https://playerlands-store-resources.s3.eu-west-1.amazonaws.com/brands/discord/discord-icon.svg" />
                    <a href="{{socials.discord}}" target="_blank" class="flex transform duration-200 ease hover:translate-x-1 cursor-pointer">
                        <p class="opacity-80 text-lg font-normal self-center">
                            Rejoindre&nbsp;notre&nbsp;Discord
                        </p>
                    </a>
                </div>
                {%endif%}
                {% if playerUsername %}
                <div class="flex flex-col lg:flex-row m-2 justify-end text-sm">
                    <div class="flex flex-col lg:flex-row gap-1">
                        <span class="secondary-background px-2 text-center py-px text-white self-center">Connecté en tant que : <span class="text-white">{{playerUsername}}</span></span>
                        <button onclick="playerlands_Logout()">
                            Changer le pseudo de compte
                        </button>
                    </div>
                </div>
                {% else %}
                <div class="flex gap-1 self-center">
                    <input placeholder="Username" class="rounded-sm text-white text-opacity-50 text-sm background-primary border-b border-white border-opacity-50 transform ease-in-out duration-75 focus:ring-2 focus:ring-purple-500 focus:outline-none focus:border-none" type="text" id="playerlands_username" />
                    <button class="self-center text-sm text-white text-opacity-50 ease-in-out duration-75 hover:text-opacity-100" onclick="playerlands_Login()">
                        Se connecter
                    </button>
                </div>
                {% endif %}
            </li>
        </ul>
    </div>
    <div class="background-secondary h-12 flex">
        <div class="flex mx-auto self-center font-semibold w-3/5 text-2xl text-primary justify-between">
            <p class="text-tertiary mx-auto transform ease duration-200 hover:-translate-y-1 cursor-pointer" onclick="{{push('/')}}">
                Magasin
            </p>
        </div>
    </div>
    <!-- blog pages from blog database -->
    <div class="justify-around background-secondary hidden md:flex">
        {%for post in blogs%}
        <p class="text-white transform ease duration-200 hover:-translate-y-1 cursor-pointer" onclick="{{ push('/blog/' + post._id)}}">
            {{post.title}}
        </p>
        {%endfor%}
    </div>
    <div class="md:hidden background-secondary">
        <div class="parent-nav-links">
            {%for post in blogs%}
            <p class="text-white transform ease duration-200 hover:-translate-y-1 cursor-pointer" onclick="{{ push('/blog/' + post._id)}}">
                {{post.title}}
            </p>
            {%endfor%}
        </div>
    </div>

    <div class="background-primary md:w-4/5 mx-auto relative rounded-sm mt-52 border-2 border-white border-opacity-5">
        <!-- store logo-->
        {%if media.logoUrl|length%}
        <img class="absolute -top-24 right-0 left-1/2 transform -translate-x-1/2 store-logo-size cursor-pointer z-50" src="{{media.logoUrl}}" onclick="{{push('/')}}" />
        {%else%}
        <img class="absolute -top-24 right-0 left-1/2 transform -translate-x-1/2 store-logo-size cursor-pointer z-50" src="{{templateSettings.storeLogo}}" onclick="{{push('/')}}" />
        {%endif%}
        <div class="flex flex-col-reverse md:flex-row justify-between text-primary font-semibold text-2xl mt-28 text-center md:text-left md:mt-0">

            <div class="md:hidden">
                {%if not basketIsEmpty %}
                <p class="text-xl cursor-pointer transform ease duration-300 hover:translate-x-1" onclick="{{ push('/checkout') }}">
                    ALLER À LA CAISSE :
                    {%if (basket.totalIncludingTax / 100).toFixed(2) > 0 %}
                    <span class="text-tertiary">{{store.currencySymbol}}{{(basket.totalIncludingTax / 100).toFixed(2)}}</span>
                    {%else%}
                    <span class="text-tertiary">FREE</span>
                    {%endif%}
                </p>
                {%else%}
                <p class="text-xl  opacity-50">
                    PANIER: <span class="text-tertiary">{{store.currencySymbol}}{{(basket.totalIncludingTax/100).toFixed(2)}}</span>
                </p>
                {%endif%}
            </div>

            <div class="flex flex-col md:mx-4 self-center">
                <!-- server player count -->
                <p class="text-tertiary">
                    {{ server.playerCount or "0" }}
                    <span class="text-primary">Joueurs en ligne :</span>
                </p>
            </div>
            <div class="flex flex-col mx-4">
                <p class="md:text-right">
                    {{ server.url}}
                </p>
                {#<p class="text-lg uppercase font-normal text-center lg:text-right">
          <span class="text-primary">Server Status</span> - {{ server.online }}
        </p>#}

                {# {% if basket.lineItems | length %}
        <p class="cursor-pointer" onclick="{{ push('/checkout')}}">{{store.currencySymbol}}
          {{(basket.totalIncludingTax).toFixed(2)}}</p>
        {%endif%} #}
                <div class="hidden md:flex-col md:flex ml-auto">
                    {%if not basketIsEmpty %}
                    <p class="text-xl cursor-pointer transform ease duration-300 hover:translate-x-1" onclick="{{ push('/checkout') }}">
                        ALLER À LA CAISSE :
                        {%if (basket.totalIncludingTax / 100).toFixed(2) > 0 %}
                        <span class="text-tertiary">{{store.currencySymbol}}{{(basket.totalIncludingTax / 100).toFixed(2)}}</span>
                        {%else%}
                        <span class="text-tertiary">GRATUIT</span>
                        {%endif%}
                    </p>
                    {%else%}
                    <p class="text-xl  opacity-50">
                        PANIER: <span class="text-tertiary">{{store.currencySymbol}}{{(basket.totalIncludingTax/100).toFixed(2)}}</span>
                    </p>
                    {%endif%}
                    <p class="text-sm text-right">
                        ITEMS X {{totalBasketItems}}
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>